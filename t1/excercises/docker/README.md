# Excercise infra

I used docker to create sample Hosts with fixed IPs and Ports then added them into inventory.

As ansible controller I am using local WSL installed with `pipx`.

## Requirements

Requirements on host system to run guest OSes.

- Ansible installed
- Docker installed
